From 4d86d02e7b4b3c75c2e8bd2b9cd0640a5caa8852 Mon Sep 17 00:00:00 2001
From: Owen Wu <twowen@gmail.com>
Date: Tue, 8 Oct 2024 14:47:02 +0800
Subject: [PATCH 3/3] Adjust code segment regarding installation for specific
 platform

Move kernel command parameter to device vendor folder

Signed-off-by: Owen Wu <twowen@gmail.com>
---
 .../installer.conf                            |  1 +
 .../installer.conf                            |  1 +
 .../installer.conf                            |  1 +
 platform/marvell/platform_arm64.conf          | 58 ++++++-------------
 4 files changed, 21 insertions(+), 40 deletions(-)
 create mode 100644 device/wistron/arm64-wistron_es1227_54ts-r0/installer.conf
 create mode 100644 device/wistron/arm64-wistron_es2227_54ts-r0/installer.conf
 create mode 100644 device/wistron/arm64-wistron_es2227_54ts_p-r0/installer.conf

diff --git a/device/wistron/arm64-wistron_es1227_54ts-r0/installer.conf b/device/wistron/arm64-wistron_es1227_54ts-r0/installer.conf
new file mode 100644
index 000000000..28757b561
--- /dev/null
+++ b/device/wistron/arm64-wistron_es1227_54ts-r0/installer.conf
@@ -0,0 +1 @@
+ONIE_PLATFORM_EXTRA_CMDLINE_LINUX="default_hugepagesz=2M hugepages=256 pci=noaer"
diff --git a/device/wistron/arm64-wistron_es2227_54ts-r0/installer.conf b/device/wistron/arm64-wistron_es2227_54ts-r0/installer.conf
new file mode 100644
index 000000000..28757b561
--- /dev/null
+++ b/device/wistron/arm64-wistron_es2227_54ts-r0/installer.conf
@@ -0,0 +1 @@
+ONIE_PLATFORM_EXTRA_CMDLINE_LINUX="default_hugepagesz=2M hugepages=256 pci=noaer"
diff --git a/device/wistron/arm64-wistron_es2227_54ts_p-r0/installer.conf b/device/wistron/arm64-wistron_es2227_54ts_p-r0/installer.conf
new file mode 100644
index 000000000..28757b561
--- /dev/null
+++ b/device/wistron/arm64-wistron_es2227_54ts_p-r0/installer.conf
@@ -0,0 +1 @@
+ONIE_PLATFORM_EXTRA_CMDLINE_LINUX="default_hugepagesz=2M hugepages=256 pci=noaer"
diff --git a/platform/marvell/platform_arm64.conf b/platform/marvell/platform_arm64.conf
index ee34e6c2b..4e1ce0b5f 100644
--- a/platform/marvell/platform_arm64.conf
+++ b/platform/marvell/platform_arm64.conf
@@ -117,19 +117,14 @@ fi
 demo_dev=${blk_dev}p${demo_part}
 
 remove_dev_partitions() {
-    if [ $PLATFORM_WISTRON -eq 1 ]; then
-        echo "Remove all existing partitions from ${blk_dev}"
-	sgdisk -Z ${blk_dev}
-    else
-        echo "Remove all existing partitions starting partnum: ${demo_part} from ${blk_dev}"
-        local dev_to_install=${blk_dev}p
-        for p in $(seq ${demo_part} 9) ; do
-            if [[ -e ${dev_to_install}${p} ]]; then
-                echo "Removing partition ${dev_to_install}${p}"
-                sgdisk -d ${p} ${blk_dev} || true
-            fi
-        done
-    fi
+    echo "Remove all existing partitions starting partnum: ${demo_part} from ${blk_dev}"
+    local dev_to_install=${blk_dev}p
+    for p in $(seq ${demo_part} 9) ; do
+        if [[ -e ${dev_to_install}${p} ]]; then
+            echo "Removing partition ${dev_to_install}${p}"
+            sgdisk -d ${p} ${blk_dev} || true
+        fi
+    done
     partprobe ${blk_dev}
 }
 
@@ -138,15 +133,9 @@ create_demo_partition() {
     remove_dev_partitions
 
     # Create sonic partition
-    if [ $PLATFORM_WISTRON -eq 1 ]; then
-        sgdisk -g --new ${demo_part}:: \
-            --change-name=${demo_part}:${demo_volume_label} \
-            --typecode=${demo_part}:8300 -p ${blk_dev}
-    else
-        sgdisk --new ${demo_part}:: \
-            --change-name=${demo_part}:${demo_volume_label} \
-            --typecode=${demo_part}:8300 -p ${blk_dev}
-    fi
+    sgdisk --new ${demo_part}:: \
+        --change-name=${demo_part}:${demo_volume_label} \
+        --typecode=${demo_part}:8300 -p ${blk_dev}
 
     partprobe
 }
@@ -210,7 +199,7 @@ prepare_boot_menu() {
         fw_setenv ${FW_ARG} fdt_name_old "" > /dev/null
         fw_setenv ${FW_ARG} fit_name_old "" > /dev/null
         fw_setenv ${FW_ARG} sonic_version_2 "None" > /dev/null
-        #fw_setenv ${FW_ARG} linuxargs_old "" > /dev/null
+        fw_setenv ${FW_ARG} linuxargs_old "" > /dev/null
     else
         CURR_SONIC_IMAGE="$(sonic-installer list | grep "Current: " | cut -f2 -d' ')"
         FIRST_SONIC_IMAGE="$(fw_printenv sonic_version_1 | cut -f2 -d'=')"
@@ -221,14 +210,14 @@ prepare_boot_menu() {
             fdt_name_old=$(fw_printenv -n fdt_name || true)
             fit_name_old=$(fw_printenv -n fit_name || true)
             sonic_version_2=$(fw_printenv -n sonic_version_1 || true)
-            #linuxargs_old=$(fw_printenv -n linuxargs || true)
+            linuxargs_old=$(fw_printenv -n linuxargs || true)
             fw_setenv ${FW_ARG} image_dir_old "$image_dir_old" > /dev/null
             fw_setenv ${FW_ARG} image_name_old "$image_name_old" > /dev/null
             fw_setenv ${FW_ARG} initrd_name_old "$initrd_name_old" > /dev/null
             fw_setenv ${FW_ARG} fdt_name_old "$fdt_name_old" > /dev/null
             fw_setenv ${FW_ARG} fit_name_old "$fit_name_old" > /dev/null
             fw_setenv ${FW_ARG} sonic_version_2 "$sonic_version_2" > /dev/null
-            #fw_setenv ${FW_ARG} linuxargs_old "$linuxargs_old" > /dev/null
+            fw_setenv ${FW_ARG} linuxargs_old "$linuxargs_old" > /dev/null
         fi
     fi
 
@@ -241,22 +230,12 @@ prepare_boot_menu() {
     fw_setenv ${FW_ARG} sonic_version_1 $demo_volume_revision_label > /dev/null
     BOOT1='echo " > Boot1: $sonic_version_1 - run sonic_image_1";echo;'
     BOOT2='echo " > Boot2: $sonic_version_2 - run sonic_image_2";echo;'
-    if [ $PLATFORM_WISTRON -eq 1 ]; then
-        BOOT3='echo " > Boot3: ONIE - run onie_bootcmd";echo;'
-    else
-        BOOT3='echo " > Boot3: ONIE - run onie_boot";echo;'
-    fi
+    BOOT3='echo " > Boot3: ONIE - run onie_boot";echo;'
     BORDER='echo "---------------------------------------------------";echo;'
     fw_setenv ${FW_ARG} print_menu "$BORDER $BOOT1 $BOOT2 $BOOT3 $BORDER" > /dev/null
 
-    if [ $PLATFORM_WISTRON -eq 1 ]; then
-        fw_setenv ${FW_ARG} linuxargs "net.ifnames=0 loopfstype=squashfs loop=$image_dir/$FILESYSTEM_SQUASHFS systemd.unified_cgroup_hierarchy=0 varlog_size=$VAR_LOG hugepagesz=2M hugepages=256 ${ONIE_PLATFORM_EXTRA_CMDLINE_LINUX}" > /dev/null
-        fw_setenv ${FW_ARG} linuxargs_old "net.ifnames=0 loopfstype=squashfs loop=$image_dir_old/$FILESYSTEM_SQUASHFS systemd.unified_cgroup_hierarchy=0 varlog_size=$VAR_LOG hugepagesz=2M hugepages=256 ${ONIE_PLATFORM_EXTRA_CMDLINE_LINUX}" > /dev/null
-    else
-        fw_setenv ${FW_ARG} linuxargs "net.ifnames=0 loopfstype=squashfs loop=$image_dir/$FILESYSTEM_SQUASHFS systemd.unified_cgroup_hierarchy=0 varlog_size=$VAR_LOG ${ONIE_PLATFORM_EXTRA_CMDLINE_LINUX}" > /dev/null
-        fw_setenv ${FW_ARG} linuxargs_old "net.ifnames=0 loopfstype=squashfs loop=$image_dir_old/$FILESYSTEM_SQUASHFS systemd.unified_cgroup_hierarchy=0 varlog_size=$VAR_LOG ${ONIE_PLATFORM_EXTRA_CMDLINE_LINUX}" > /dev/null
-    fi
-    sonic_bootargs_old='setenv bootargs root='$demo_dev' rw rootwait rootfstype=ext4 panic=1 console=ttyS0,${baudrate} ${othbootargs} ${mtdparts} ${linuxargs_old}'
+    fw_setenv ${FW_ARG} linuxargs "net.ifnames=0 loopfstype=squashfs loop=$image_dir/$FILESYSTEM_SQUASHFS systemd.unified_cgroup_hierarchy=0 varlog_size=$VAR_LOG ${ONIE_PLATFORM_EXTRA_CMDLINE_LINUX}" > /dev/null
+    sonic_bootargs_old='setenv bootargs root='$demo_dev' rw rootwait panic=1 console=ttyS0,${baudrate} ${othbootargs} ${mtdparts} ${linuxargs_old}'
     fw_setenv ${FW_ARG} sonic_bootargs_old "$sonic_bootargs_old" > /dev/null || true
     sonic_boot_load_old=$(fw_printenv -n sonic_boot_load || true)
     old_str="_old"
@@ -276,9 +255,8 @@ prepare_boot_menu() {
         SONIC_BOOT_CMD='run sonic_bootargs; run sonic_boot_load; bootm $fit_addr${fit_conf_name}'
         SONIC_BOOT_CMD_OLD='run sonic_bootargs_old; run sonic_boot_load_old; bootm $fit_addr${fit_conf_name}'
     fi
-    BOOTARGS='setenv bootargs root='$demo_dev' rw rootwait rootfstype=ext4 panic=1 console=ttyS0,${baudrate} ${othbootargs} ${mtdparts} ${linuxargs}'
+    BOOTARGS='setenv bootargs root='$demo_dev' rw rootwait panic=1 console=ttyS0,${baudrate} ${othbootargs} ${mtdparts} ${linuxargs}'
     fw_setenv ${FW_ARG} sonic_bootargs "$BOOTARGS" > /dev/null
-    fw_setenv ${FW_ARG} sonic_bootcmd "$SONIC_BOOT_CMD" > /dev/null
     fw_setenv ${FW_ARG} sonic_image_2 "$SONIC_BOOT_CMD_OLD" > /dev/null
     fw_setenv ${FW_ARG} sonic_image_1 "$SONIC_BOOT_CMD" > /dev/null
     fw_setenv ${FW_ARG} boot_next  'run sonic_image_1'> /dev/null
-- 
2.25.1

